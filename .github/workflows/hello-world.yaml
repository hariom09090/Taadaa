name: Hello World Workflow

on:
  push:
    branches:
      - github-actions  # or any branch you’re testing on

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello World
        run: echo "🎉 Hello World from GitHub Actions!"
